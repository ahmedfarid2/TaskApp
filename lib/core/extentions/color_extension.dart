import 'package:flutter/material.dart';

extension ColorExtension on ColorScheme {
  // Primary Colors
  Color get primaryColor => const Color(0xFF9fc749);

  Color get whiteColor => const Color(0xFFffffff);
  Color get blackColor => const Color(0xFF000000);

  // Danger Colors
  Color get dangerColor => const Color(0xffff002e);

}
